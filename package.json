{
  "name": "chem-phone",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.0.1"
  },
  "scripts": {
    "dev": "lerna run --scope='{server,client}' --parallel start:dev --stream"
  }
}
