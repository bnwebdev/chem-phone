{
  "name": "chem-phone",
  "private": true,
  "workspaces": [
    "packages/*",
    "modules/**/*"
  ],
  "devDependencies": {
    "lerna": "^6.0.1"
  },
  "scripts": {
    "dev": "lerna run --scope='{server,client}' --parallel start:dev --stream",
    "bootstrap": "lerna bootstrap"
  },
  "dependencies": {
    "i18next": "^21.10.0"
  }
}
